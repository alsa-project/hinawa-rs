[options]
girs_directories = ["../gir-files", ".."]
work_mode = "sys"
library = "Hinawa"
version = "4.0"
min_cfg_version = "4.0"
single_version_file = "."
external_libraries = [
  "GLib",
  "GObject",
]
